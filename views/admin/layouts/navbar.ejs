<div class="nav">
    <input type="checkbox" id="nav-check">
    <div class="nav-header">
        <div class="nav-title">
            <a href="/"style="color: white;"><i class="fas fa-book-reader"></i> MangaLife</a>
        </div>
    </div>
    <div class="nav-btn">
        <label for="nav-check">
            <span></span>
            <span></span>
            <span></span>
        </label>
    </div>

    <div class="nav-links">
        <a href="/mangas" >Listes des Mangas</a>
        <%if (!utilities.empty(locals.user)) {%>

        <%if (!utilities.empty(locals.user.email)) { %>
            <a href="/list/favoris">Mes favoris</a>
            <a href="/profil"><%=locals.user.username%></a>
            <a href="/logout">Déconnexion</a>
        <%}%>


        <%}else{%>
       
            <a href="/login">Connexion</a>

        <%}%>
   

        <input type="text" id="search" placeholder="Rechercher un mangas">
    </div>
</div>